import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["tse1.mm.bing.net", "www.somosmamas.com.ar", "cloudinary.com", "imgur.com"],
  },
};

export default nextConfig;
